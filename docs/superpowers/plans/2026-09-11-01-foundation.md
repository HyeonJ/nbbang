# 엔빵 Plan 01 — 기반: 스캐폴드·DB·인증·모임/멤버 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 배포 가능한 뼈대 — 가입/로그인, 모임 개설, 초대 링크 합류, 모임 목록·대시보드 스켈레톤·설정까지 (F1 완결).

**Architecture:** 단일 Next.js 15 앱. 조회는 RSC에서 `lib/db` 직접, 쓰기는 next-safe-action 클라이언트 3단(base→auth→group)으로 ADR-002 인가를 강제. Drizzle+Neon, Better Auth. UI는 기능 최소(Tailwind 기본) — 디자인 라운드는 Plan 02 직전 게이트.

**Tech Stack:** Next.js 15 · React 19 · TS · Drizzle ORM · Neon(@neondatabase/serverless) · Better Auth · next-safe-action v8 · zod · Vitest · Playwright.

**참조 문서:** `docs/requirements.md`(F1), `docs/superpowers/specs/2026-09-11-nbbang-design.md` §1–3, `docs/adr/002`, `docs/glossary.md`(네이밍 준수).

**⚠️ 사용자 지원 게이트 2곳:** Task 2 시작 전 Neon `DATABASE_URL`(dev·test 2개), Task 9 Vercel 연결. 없으면 해당 태스크는 NEEDS_CONTEXT로 보고하고 컨트롤러가 사용자에게 요청한다.

**작업 위치:** `C:\Users\bpx27\orca\projects\nbbang`, `main` 직접 작업(첫 스캐폴드 단계라 브랜치 불필요 — Task 4부터는 기능 브랜치 선택 가능하나 v1 솔로 개발이므로 main 커밋 허용). PowerShell.

---

## 파일 구조 (Plan 01이 만드는 것)

```
nbbang/
├── app/
│   ├── layout.tsx, page.tsx          # 랜딩(최소)
│   ├── (auth)/login/page.tsx         # 로그인+가입 탭
│   ├── api/auth/[...all]/route.ts    # Better Auth 핸들러
│   ├── groups/page.tsx               # 내 모임 목록 + 생성
│   ├── groups/[groupId]/page.tsx     # 대시보드 스켈레톤
│   ├── groups/[groupId]/settings/page.tsx
│   └── invite/[token]/page.tsx       # 초대 합류
├── actions/
│   ├── clients.ts                    # ★ safe-action 3단 클라이언트 (ADR-002)
│   ├── group.ts                      # createGroup, regenerateInviteToken
│   └── membership.ts                 # joinByInvite
├── lib/
│   ├── auth.ts / auth-client.ts      # Better Auth 서버/클라이언트
│   ├── db/index.ts                   # Drizzle+Neon 클라이언트
│   ├── db/schema.ts                  # groups, memberships (+Better Auth 생성분)
│   └── domain/token.ts               # 토큰 생성 (순수, 테스트 대상)
├── e2e/foundation.spec.ts
├── drizzle.config.ts, vitest.config.mts, playwright.config.ts
└── .github/workflows/ci.yml
```

---

### Task 1: 스캐폴드 + 테스트 인프라 + CI

**Files:** Create: 프로젝트 스캐폴드 전체, `vitest.config.mts`, `.github/workflows/ci.yml`. Modify: `README.md`(개발 섹션), `.gitignore`

- [ ] **Step 1: create-next-app** — 기존 파일(README, docs, .gitattributes)이 있으므로 임시 폴더에 생성 후 병합:
```powershell
cd C:\Users\bpx27\orca\projects
npx create-next-app@latest nbbang-tmp --ts --tailwind --eslint --app --src-dir=false --import-alias "@/*" --use-npm --turbopack --yes
# nbbang-tmp의 내용물을 nbbang/으로 이동 (README.md는 기존 것 유지 — tmp의 README는 버림)
Get-ChildItem nbbang-tmp -Force | Where-Object { $_.Name -notin @('README.md','.git') } | Move-Item -Destination nbbang -Force
Remove-Item -Recurse -Force nbbang-tmp
cd nbbang; npm run dev # 켜지는지 확인 후 종료
```
- [ ] **Step 2: Vitest** — `npm i -D vitest @vitejs/plugin-react`; `vitest.config.mts`:
```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  test: { include: ['lib/**/*.test.ts', 'actions/**/*.test.ts'], environment: 'node' },
  resolve: { alias: { '@': path.resolve(__dirname, '.') } },
});
```
`package.json` scripts에 `"test": "vitest run"` 추가. 스모크 테스트 `lib/smoke.test.ts`(`expect(1+1).toBe(2)`) 작성 → `npm test` 1 passing 확인 → 파일 삭제는 하지 말고 Task 3에서 실제 테스트가 생기면 제거.
- [ ] **Step 3: CI** — `.github/workflows/ci.yml`:
```yaml
name: CI
on: { push: { branches: [main] }, pull_request: {} }
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm test
# E2E job은 Task 8에서 추가 (DB 시크릿 필요)
```
- [ ] **Step 4: README 개발 섹션** — "(스캐폴드 후 작성)" 자리를 실제 명령(dev/lint/test)으로 교체.
- [ ] **Step 5: 검증·커밋** — `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build` 전부 통과 후:
```powershell
git add -A; git commit -m "feat: Next.js 15 스캐폴드 + Vitest + CI"
```

### Task 2: Drizzle + Neon + 스키마 (groups, memberships)

**Files:** Create: `lib/db/index.ts`, `lib/db/schema.ts`, `drizzle.config.ts`, `.env.local`(untracked), `.env.example`. Test: 스키마는 마이그레이션 적용으로 검증

**게이트:** `.env.local`에 `DATABASE_URL`(dev), `.env.test`에 `DATABASE_URL`(test 브랜치) 필요. 없으면 NEEDS_CONTEXT 보고.

- [ ] **Step 1: 의존성** — `npm i drizzle-orm @neondatabase/serverless; npm i -D drizzle-kit dotenv`
- [ ] **Step 2: `.env.example`** (tracked):
```
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
BETTER_AUTH_SECRET=openssl-rand-32
BETTER_AUTH_URL=http://localhost:3000
```
- [ ] **Step 3: `drizzle.config.ts`**:
```ts
import 'dotenv/config';
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './lib/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: { url: process.env.DATABASE_URL! },
});
```
(주의: drizzle-kit은 `.env` 로드 — `.env.local`만 있으면 `dotenv -e` 또는 `.env` 심볼릭 대신 `package.json` 스크립트 `"db:push": "dotenv -e .env.local -- drizzle-kit push"` 방식 사용, `npm i -D dotenv-cli`.)
- [ ] **Step 4: `lib/db/schema.ts`** — glossary 네이밍 준수:
```ts
import { pgTable, text, timestamp, integer, uniqueIndex } from 'drizzle-orm/pg-core';

export const groups = pgTable('groups', {
  id: text('id').primaryKey(), // crypto.randomUUID()
  name: text('name').notNull(),
  inviteToken: text('invite_token').notNull().unique(),
  publicToken: text('public_token').notNull().unique(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});

export const memberships = pgTable('memberships', {
  id: text('id').primaryKey(),
  userId: text('user_id').notNull(), // Better Auth user.id 참조 (FK는 auth 스키마 생성 후 Task 3에서 추가)
  groupId: text('group_id').notNull().references(() => groups.id),
  role: text('role', { enum: ['owner', 'member'] }).notNull(),
  displayName: text('display_name').notNull(),
  joinedAt: timestamp('joined_at', { withTimezone: true }).notNull().defaultNow(),
}, (t) => [uniqueIndex('memberships_user_group').on(t.userId, t.groupId)]);
```
- [ ] **Step 5: `lib/db/index.ts`**:
```ts
import { neon } from '@neondatabase/serverless';
import { drizzle } from 'drizzle-orm/neon-http';
import * as schema from './schema';

const sql = neon(process.env.DATABASE_URL!);
export const db = drizzle(sql, { schema });
```
- [ ] **Step 6: 마이그레이션 적용·검증** — `npm run db:push` → 성공 출력 확인, `drizzle-kit push`가 만든 테이블을 `psql`/Neon 콘솔 대신 코드로 확인: 임시 스크립트로 `select 1` + `db.select().from(groups)` 실행(빈 배열). 커밋 `feat: Drizzle+Neon — groups/memberships 스키마`.

### Task 3: Better Auth (가입·로그인)

**Files:** Create: `lib/auth.ts`, `lib/auth-client.ts`, `app/api/auth/[...all]/route.ts`, `app/(auth)/login/page.tsx`, `lib/db/auth-schema.ts`(CLI 생성). Modify: `lib/db/schema.ts`(re-export), `.env.local`(secret)

- [ ] **Step 1:** `npm i better-auth`
- [ ] **Step 2: `lib/auth.ts`**:
```ts
import { betterAuth } from 'better-auth';
import { drizzleAdapter } from 'better-auth/adapters/drizzle';
import { db } from '@/lib/db';

export const auth = betterAuth({
  database: drizzleAdapter(db, { provider: 'pg' }),
  emailAndPassword: { enabled: true },
});
```
- [ ] **Step 3: 스키마 생성** — `npx @better-auth/cli@latest generate --config lib/auth.ts --output lib/db/auth-schema.ts` → 생성물(user/session/account/verification)을 `lib/db/schema.ts`에서 `export * from './auth-schema';`로 합류 → `npm run db:push`. (CLI 플래그가 버전에 따라 다르면 `--help`로 확인하고 실제 사용 커맨드를 보고에 기록.)
- [ ] **Step 4: 핸들러·클라이언트**:
```ts
// app/api/auth/[...all]/route.ts
import { auth } from '@/lib/auth';
import { toNextJsHandler } from 'better-auth/next-js';
export const { GET, POST } = toNextJsHandler(auth);
```
```ts
// lib/auth-client.ts
import { createAuthClient } from 'better-auth/react';
export const authClient = createAuthClient();
```
- [ ] **Step 5: 로그인/가입 페이지** — `app/(auth)/login/page.tsx` (client component): 이메일·비밀번호·(가입 시 이름) 폼, `authClient.signUp.email` / `authClient.signIn.email` 호출, 성공 시 `/groups`로. 스타일은 Tailwind 최소(가운데 카드 폼). 에러는 폼 아래 텍스트.
- [ ] **Step 6: 검증·커밋** — dev 서버에서 가입→로그인→쿠키 세션 확인(curl로 `/api/auth/get-session` 200+user). lint/tsc/build. 커밋 `feat: Better Auth 이메일 가입·로그인`.

### Task 4: 액션 인프라 — safe-action 3단 클라이언트 (ADR-002 구현)

**Files:** Create: `actions/clients.ts`, `lib/domain/token.ts`, `lib/domain/token.test.ts`

- [ ] **Step 1:** `npm i next-safe-action zod`
- [ ] **Step 2: 실패 테스트 먼저** — `lib/domain/token.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { newToken } from '@/lib/domain/token';

describe('newToken', () => {
  it('URL-safe 12+ chars, unique', () => {
    const a = newToken(); const b = newToken();
    expect(a).toMatch(/^[A-Za-z0-9_-]{12,}$/);
    expect(a).not.toBe(b);
  });
});
```
`npm test` → FAIL 확인.
- [ ] **Step 3: `lib/domain/token.ts`**:
```ts
import { randomBytes } from 'node:crypto';
export function newToken(): string {
  return randomBytes(9).toString('base64url'); // 12 chars
}
```
`npm test` → PASS. (이때 Task 1의 smoke.test.ts 삭제)
- [ ] **Step 4: `actions/clients.ts`** — 3단 클라이언트:
```ts
import { createSafeActionClient } from 'next-safe-action';
import { headers } from 'next/headers';
import { and, eq } from 'drizzle-orm';
import { z } from 'zod';
import { auth } from '@/lib/auth';
import { db } from '@/lib/db';
import { memberships } from '@/lib/db/schema';

export class ActionError extends Error {
  constructor(public code: string) { super(code); }
}

export const actionClient = createSafeActionClient({
  handleServerError(e) {
    if (e instanceof ActionError) return e.code;
    console.error(e);
    return 'INTERNAL_ERROR';
  },
});

export const authActionClient = actionClient.use(async ({ next }) => {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) throw new ActionError('UNAUTHENTICATED');
  return next({ ctx: { userId: session.user.id } });
});

// groupId를 bindArgs가 아닌 입력 스키마에 포함하는 액션이 사용.
// 사용법: groupActionClient.inputSchema(z.object({ groupId: z.string(), ... }))
export const groupActionClient = authActionClient.use(async ({ next, ctx, clientInput }) => {
  const parsed = z.object({ groupId: z.string() }).safeParse(clientInput);
  if (!parsed.success) throw new ActionError('INVALID_INPUT');
  const m = await db.query.memberships.findFirst({
    where: and(eq(memberships.groupId, parsed.data.groupId), eq(memberships.userId, ctx.userId)),
  });
  if (!m) throw new ActionError('NOT_MEMBER');
  return next({ ctx: { ...ctx, groupId: parsed.data.groupId, role: m.role, membershipId: m.id } });
});

export function assertOwner(role: string) {
  if (role !== 'owner') throw new ActionError('FORBIDDEN');
}
```
(next-safe-action v8에서 미들웨어의 `clientInput` 접근이 다르면 공식 문서의 동등 패턴으로 구현하고 보고에 기록 — 요건은 "그룹 대상 액션은 멤버십·역할 컨텍스트 없이는 도메인에 도달할 수 없다"이다.)
- [ ] **Step 5: 검증·커밋** — lint/tsc/`npm test`(token 1). 커밋 `feat: 액션 인프라 — 3단 safe-action 클라이언트(ADR-002)`.

### Task 5: 모임 생성 + 내 모임 목록

**Files:** Create: `actions/group.ts`, `app/groups/page.tsx`. Test: E2E(Task 8)

- [ ] **Step 1: `actions/group.ts`**:
```ts
'use server';
import { z } from 'zod';
import { authActionClient, groupActionClient, assertOwner } from './clients';
import { db } from '@/lib/db';
import { groups, memberships } from '@/lib/db/schema';
import { eq } from 'drizzle-orm';
import { newToken } from '@/lib/domain/token';
import { revalidatePath } from 'next/cache';

export const createGroup = authActionClient
  .inputSchema(z.object({
    name: z.string().min(1).max(50),
    displayName: z.string().min(1).max(20),
  }))
  .action(async ({ parsedInput, ctx }) => {
    const groupId = crypto.randomUUID();
    await db.insert(groups).values({
      id: groupId, name: parsedInput.name,
      inviteToken: newToken(), publicToken: newToken(),
    });
    await db.insert(memberships).values({
      id: crypto.randomUUID(), userId: ctx.userId, groupId,
      role: 'owner', displayName: parsedInput.displayName,
    });
    revalidatePath('/groups');
    return { groupId };
  });

export const regenerateInviteToken = groupActionClient
  .inputSchema(z.object({ groupId: z.string() }))
  .action(async ({ ctx }) => {
    assertOwner(ctx.role);
    const token = newToken();
    await db.update(groups).set({ inviteToken: token }).where(eq(groups.id, ctx.groupId));
    revalidatePath(`/groups/${ctx.groupId}/settings`);
    return { inviteToken: token };
  });
```
(neon-http 드라이버는 트랜잭션 미지원 — createGroup의 2-insert는 Plan 02에서 원장 도입 시 `neon-serverless`(WebSocket) 드라이버 전환과 함께 트랜잭션으로 감싼다. 이 결정을 코드 주석으로 남길 것.)
- [ ] **Step 2: `app/groups/page.tsx`** — RSC: 세션 확인(없으면 `/login` redirect), 내 멤버십 join 모임 목록, "모임 만들기" 폼(client 서브컴포넌트, `useAction(createGroup)` → 성공 시 `router.push(/groups/${groupId})`).
- [ ] **Step 3: 검증·커밋** — dev에서 생성→목록 노출 수동 확인. lint/tsc/build. 커밋 `feat: 모임 생성·목록(F1 전반)`.

### Task 6: 초대 링크 합류

**Files:** Create: `actions/membership.ts`, `app/invite/[token]/page.tsx`

- [ ] **Step 1: `actions/membership.ts`** — `joinByInvite`:
```ts
'use server';
import { z } from 'zod';
import { authActionClient, ActionError } from './clients';
import { db } from '@/lib/db';
import { groups, memberships } from '@/lib/db/schema';
import { and, eq } from 'drizzle-orm';

export const joinByInvite = authActionClient
  .inputSchema(z.object({ token: z.string(), displayName: z.string().min(1).max(20) }))
  .action(async ({ parsedInput, ctx }) => {
    const group = await db.query.groups.findFirst({ where: eq(groups.inviteToken, parsedInput.token) });
    if (!group) throw new ActionError('INVALID_INVITE');
    const dup = await db.query.memberships.findFirst({
      where: and(eq(memberships.groupId, group.id), eq(memberships.userId, ctx.userId)),
    });
    if (dup) return { groupId: group.id, already: true };
    await db.insert(memberships).values({
      id: crypto.randomUUID(), userId: ctx.userId, groupId: group.id,
      role: 'member', displayName: parsedInput.displayName,
    });
    return { groupId: group.id, already: false };
  });
```
- [ ] **Step 2: `app/invite/[token]/page.tsx`** — RSC: 토큰으로 모임 조회(없으면 "만료된 링크"), 비로그인 시 `/login?next=/invite/[token]` 유도(로그인 페이지가 `next` 쿼리 지원하도록 보강), 로그인 상태면 displayName 폼 → `joinByInvite` → 대시보드로.
- [ ] **Step 3: 검증·커밋** — 두 계정으로 수동 확인. 커밋 `feat: 초대 링크 합류(F1 완결)`.

### Task 7: 대시보드 스켈레톤 + 설정

**Files:** Create: `app/groups/[groupId]/page.tsx`, `app/groups/[groupId]/settings/page.tsx`, `lib/db/queries.ts`

- [ ] **Step 1: `lib/db/queries.ts`** — `getGroupForMember(groupId, userId)`: 멤버십 확인 포함 조회(비멤버면 null → 페이지에서 notFound()). `getGroupMembers(groupId)`.
- [ ] **Step 2: 대시보드** — 모임명, 잔액 `0원`(Plan 02에서 원장 합산으로 교체 예정 주석), 멤버 수, "회비·지출·정산은 준비 중" 섹션 자리. 설정 링크(owner만 노출).
- [ ] **Step 3: 설정** — 멤버 목록(displayName·role·joinedAt), 초대 링크 표시+복사 버튼+재발급(owner만, `regenerateInviteToken`).
- [ ] **Step 4: 검증·커밋** — 비멤버 접근 시 404, member에게 설정 버튼 미노출+액션 직접 호출도 FORBIDDEN(수동 curl로 확인). 커밋 `feat: 대시보드 스켈레톤·설정`.

### Task 8: E2E + CI 통합

**Files:** Create: `playwright.config.ts`, `e2e/foundation.spec.ts`. Modify: `.github/workflows/ci.yml`, `package.json`

- [ ] **Step 1:** `npm i -D @playwright/test dotenv-cli; npx playwright install chromium`
- [ ] **Step 2: `playwright.config.ts`** — `.env.test`의 DATABASE_URL로 `npm run build && npm run start`를 webServer로. 테스트 전 글로벌 셋업에서 test DB를 `db:push`로 동기화하고 테이블 truncate.
- [ ] **Step 3: `e2e/foundation.spec.ts`** — 시나리오 1개로 F1 전체:
```ts
import { test, expect } from '@playwright/test';

test('가입→모임 생성→초대 합류→설정 권한', async ({ browser }) => {
  const owner = await browser.newContext(); const op = await owner.newPage();
  const ts = Date.now();
  // 총무 가입 + 모임 생성
  await op.goto('/login');
  // ...가입 폼 (owner-${ts}@test.local / password123! / 민지)
  // ...모임 만들기 (이름: 테스트모임, displayName: 민지)
  await expect(op).toHaveURL(/\/groups\/.+/);
  // 설정에서 초대 링크 추출
  await op.goto(op.url() + '/settings');
  const invite = await op.getByTestId('invite-link').innerText();
  // 멤버 가입 + 합류
  const member = await browser.newContext(); const mp = await member.newPage();
  await mp.goto(invite);
  // ...로그인 유도 → 가입 → displayName 철수 → 합류
  await expect(mp).toHaveURL(/\/groups\/.+/);
  // 멤버에겐 설정 링크 없음
  await expect(mp.getByRole('link', { name: '설정' })).toHaveCount(0);
});
```
(폼 셀렉터는 구현된 실제 DOM에 맞춰 채우되, data-testid를 폼 요소에 부여해 안정화. 위 골격의 검증 포인트는 유지.)
- [ ] **Step 4: CI에 e2e job 추가** — `DATABASE_URL`(test)·`BETTER_AUTH_SECRET`을 GitHub secrets로. 시크릿 등록은 `gh secret set` 사용(값은 .env.test에서). PR에도 실행.
- [ ] **Step 5: 검증·커밋** — 로컬 `npm run e2e` 통과, push 후 CI 그린 확인. 커밋 `test: F1 E2E + CI e2e job`.

> **구현 노트(2026-09-14): E2E DB 가드 2중화**
> `e2e/global-setup.ts`는 TRUNCATE 전에 두 가드를 순서대로 통과해야 한다.
> 1. **센티널 테이블(주 가드)** — `select to_regclass('e2e_guard.sentinel') is not null`. 없으면 throw.
>    `drizzle-kit push`가 절대 만들지 않는 마커라서 URL을 어떤 표기로 적었든 "이 DB가 E2E 전용인가"를 DB 자체에 묻는다.
> 2. **URL 정규화 비교(보조 가드)** — `.env.local`(dev)이 있으면 `hostname.replace('-pooler','') + pathname` 키로 비교해
>    같은 DB면 throw. 문자열 `===` 비교는 pooled/직접 호스트·쿼리 파라미터 차이를 놓쳐서 뚫린다.
>    CI에는 `.env.local`이 없다(테스트 DB 시크릿만 보관) → 파일이 없으면 이 검사는 건너뛴다.
>
> **새 테스트 브랜치를 만들 때마다 1회 실행 필요:**
> ```sql
> create schema if not exists e2e_guard;
> create table if not exists e2e_guard.sentinel();
> ```
> 현재 `secrets.TEST_DATABASE_URL`이 가리키는 브랜치에는 생성 완료. dev 브랜치에는 절대 만들지 말 것 —
> 센티널이 없다는 사실 자체가 dev를 지켜준다. 브랜치 교체 시 이 단계를 빠뜨리면 E2E는 셋업 단계에서 실패한다.
>
> **센티널이 별도 스키마에 사는 이유(2026-09-14 변경):** `drizzle-kit push`는 `public` 스키마만 관리한다.
> 센티널이 `public.e2e_sentinel`이던 시절에는 push가 이를 매번 '삭제 대상'으로 잡았고,
> 같은 push에 '생성 대상' 테이블이 하나라도 있으면 drizzle-kit이 "새 테이블인가 rename인가"를
> 대화형으로 물어 비-TTY(CI)에서 죽었다(`--force`도 이 프롬프트는 건너뛰지 못한다).
> 그래서 global-setup이 push 전에 drop하고 후에 다시 세우는 우회를 했는데, 그 사이에서 중단되면 센티널이 사라졌다.
> `e2e_guard` 스키마로 옮기면 push의 시야 밖이라 drop/재생성 자체가 필요 없고, 프롬프트도 영구히 사라진다.
> 현재 global-setup의 순서는 **센티널 검사 → URL 정규화 비교 → push → TRUNCATE** 뿐이다.

### Task 9: Vercel 배포

**Files:** Modify: `README.md`(배포 URL)

- [ ] **Step 1:** `vercel` CLI 또는 대시보드로 `HyeonJ/nbbang` 연결(사용자 지원 게이트 — 로그인 필요 시 NEEDS_CONTEXT). env: `DATABASE_URL`(prod는 dev와 같은 Neon이라도 v1 허용), `BETTER_AUTH_SECRET`(새로 생성), `BETTER_AUTH_URL`(배포 도메인).
- [ ] **Step 2:** 프로덕션 스모크 — `/login` 200, 가입→모임 생성 1회 실제 수행, `/api/auth/get-session` 동작.
- [ ] **Step 3:** README에 배포 URL 기록, 커밋 `docs: 배포 URL`.

---

## Self-Review

- **스펙 커버리지**: F1(모임·멤버·초대·역할) 전부 태스크로 매핑 ✓. F2~F7은 Plan 02/03 범위(스펙 §6 분할 결정과 일치). ADR-002는 Task 4가 구현, ADR-001은 Plan 02(원장)에서.
- **플레이스홀더 스캔**: 로그인 페이지·E2E 폼 셀렉터는 "구현 DOM에 맞춰 + data-testid 안정화"로 의도적 위임 — 검증 포인트는 명시했으므로 통과. Better Auth CLI 플래그·safe-action v8 미들웨어 시그니처는 버전 드리프트 가능성을 명시하고 보고 의무를 붙임.
- **타입 일관성**: `newToken`(T4 정의→T5·T6 사용), `ActionError`(T4→T5·T6), `assertOwner`(T4→T5), glossary 네이밍(memberships.displayName, inviteToken/publicToken) 일치 ✓.
- **알려진 기술 부채(의도)**: neon-http 트랜잭션 미지원 — Plan 02 원장 도입 시 드라이버 전환+트랜잭션으로 해소(Task 5에 주석 의무).
