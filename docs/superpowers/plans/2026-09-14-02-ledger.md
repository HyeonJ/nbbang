# 엔빵 Plan 02 — 원장·회비·지출 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 돈이 실제로 흐른다 — 불변 원장 위에 지출 기록·역분개, 회비 회차·납부 체크를 얹고, 대시보드 잔액을 원장 합산으로 바꾼다 (F2·F3 완결).

**Architecture:** ADR-001의 append-only 원장이 유일한 금액 진실. `ledger_entries`에 **부호 있는 금액**을 적재하고 잔액은 합산으로 파생한다(저장 금지). 정정은 REVERSAL 엔트리. 2개 이상 테이블을 함께 바꾸는 쓰기(납부 체크, 모임 생성)는 트랜잭션으로 묶으며, 이를 위해 DB 드라이버를 neon-http → **neon-serverless(WebSocket)** 로 교체한다. 도메인 규칙은 `lib/domain/`의 순수 함수에 두고 Vitest로 고정한다.

**Tech Stack:** Plan 01과 동일 + `ws`(neon-serverless 의존), 디자인 시스템 컴포넌트(`components/ui/`).

**참조:** `docs/requirements.md`(F2·F3), `docs/superpowers/specs/2026-09-11-nbbang-design.md` §2–4, `docs/adr/001-immutable-ledger.md`, `docs/adr/002-server-action-authorization.md`, `docs/glossary.md`, `docs/design/direction.md`(스위스 그리드 토큰).

**⚠️ 사용자 게이트 1곳:** Task 2에서 화면 시안(HTML 프로토타입) 승인. 컨트롤러가 시안을 만들어 사용자에게 보내고, 승인 전에는 Task 2 이후 UI 태스크를 시작하지 않는다.

**작업 위치:** `C:\Users\bpx27\orca\projects\nbbang`, `main` 직접 커밋(솔로 개발, Plan 01과 동일). PowerShell. 모든 태스크는 lint/tsc/`npm test`/`npm run build` 통과 후 커밋·푸시하고 CI(`check`·`e2e`) 그린을 확인한다.

---

## 파일 구조 (Plan 02가 만드는/바꾸는 것)

```
lib/db/index.ts                  neon-serverless 드라이버 + db.transaction 지원 (교체)
lib/db/schema.ts                 + ledgerEntries, duesRounds, duesPayments
lib/db/queries.ts                + 원장·회차 조회 (확장)
lib/domain/ledger.ts             부호 규칙·잔액 합산·역분개 검증 (순수) + .test.ts
lib/domain/dues.ts               회차 금액·미납자 산출 (순수) + .test.ts
lib/format.ts                    금액·날짜(KST) 표기 (순수) + .test.ts
actions/ledger.ts                createExpense, reverseEntry
actions/dues.ts                  createRound, markPaid, unmarkPaid
components/ui/                   Button, Field, Amount, DataTable, PageHeader, SectionTitle
app/groups/[groupId]/page.tsx            대시보드 — 잔액=원장 합산 (교체)
app/groups/[groupId]/expenses/page.tsx   지출 목록·입력
app/groups/[groupId]/dues/page.tsx       회차 목록·생성
app/groups/[groupId]/dues/[roundId]/page.tsx  납부 체크 그리드
e2e/ledger.spec.ts               지출·역분개·회비 흐름
```

---

### Task 1: 드라이버 전환 + 트랜잭션 + Plan 01 부채 정리

**Files:** Modify `lib/db/index.ts`, `actions/group.ts`, `actions/membership.ts`, `package.json`. Create `lib/format.ts`, `lib/format.test.ts`. Modify `app/groups/page.tsx`, `app/groups/[groupId]/settings/page.tsx`(날짜 표기)

- [ ] **Step 1: 의존성** — `npm i ws; npm i -D @types/ws`
- [ ] **Step 2: `lib/db/index.ts` 전체 교체** — WebSocket 드라이버로 바꿔 `db.transaction()`을 쓸 수 있게 한다:
```ts
import { Pool, neonConfig } from '@neondatabase/serverless';
import { drizzle } from 'drizzle-orm/neon-serverless';
import ws from 'ws';
import * as schema from './schema';

// neon-serverless는 WebSocket으로 붙어 트랜잭션을 지원한다(neon-http는 불가).
// Node 런타임에는 전역 WebSocket이 없어 ws를 주입한다.
neonConfig.webSocketConstructor = ws;

const pool = new Pool({ connectionString: process.env.DATABASE_URL! });
export const db = drizzle(pool, { schema });
```
- [ ] **Step 3: 기존 동작 회귀 확인** — `npm run build`, `npm run e2e`(1 passing). 드라이버 교체만으로 F1이 그대로 도는지 확인한다. 실패하면 원인을 보고하고 진행을 멈춘다.
- [ ] **Step 4: `createGroup` 원자화** — `actions/group.ts`의 2-insert를 트랜잭션으로 감싸고, Plan 01의 "neon-http는 트랜잭션 불가" 주석을 삭제한다:
```ts
    const groupId = crypto.randomUUID();
    await db.transaction(async (tx) => {
      await tx.insert(groups).values({
        id: groupId, name: parsedInput.name,
        inviteToken: newToken(), publicToken: newToken(),
      });
      await tx.insert(memberships).values({
        id: crypto.randomUUID(), userId: ctx.userId, groupId,
        role: 'owner', displayName: parsedInput.displayName,
      });
    });
```
- [ ] **Step 5: `joinByInvite` 레이스 패자 처리** — 유니크 위반(23505)을 멱등 성공으로 바꾼다(Task 6 리뷰 후속):
```ts
    try {
      await db.insert(memberships).values({
        id: crypto.randomUUID(), userId: ctx.userId, groupId: group.id,
        role: 'member', displayName: parsedInput.displayName,
      });
    } catch (e) {
      // 동시 합류 레이스: 유니크 인덱스(memberships_user_group)가 최종 방어선.
      // 진 쪽도 "이미 멤버"와 같은 결과로 본다.
      if ((e as { code?: string }).code === '23505') {
        revalidatePath('/groups');
        return { groupId: group.id, already: true };
      }
      throw e;
    }
```
- [ ] **Step 6: 실패 테스트 먼저 — `lib/format.test.ts`**:
```ts
import { describe, it, expect } from 'vitest';
import { formatAmount, formatDateKst } from '@/lib/format';

describe('formatAmount', () => {
  it('정수 원을 천단위 구분해 표기한다', () => {
    expect(formatAmount(360000)).toBe('360,000');
    expect(formatAmount(0)).toBe('0');
  });
  it('음수는 마이너스 기호(−)로 표기한다', () => {
    expect(formatAmount(-96000)).toBe('−96,000');
  });
});

describe('formatDateKst', () => {
  it('UTC 자정 직전도 KST 날짜로 변환한다', () => {
    // 2026-01-31T23:30:00Z = KST 2026-02-01 08:30
    expect(formatDateKst(new Date('2026-01-31T23:30:00Z'))).toBe('2026-02-01');
  });
  it('KST 자정 직후는 그날로 남는다', () => {
    // 2026-01-31T15:00:00Z = KST 2026-02-01 00:00
    expect(formatDateKst(new Date('2026-01-31T15:00:00Z'))).toBe('2026-02-01');
  });
});
```
`npm test` → FAIL 확인.
- [ ] **Step 7: `lib/format.ts`**:
```ts
const KST_OFFSET_MS = 9 * 60 * 60 * 1000;

/** 정수 원 → 천단위 구분 문자열. 음수는 U+2212(−)로 표기해 하이픈과 구분한다. */
export function formatAmount(amount: number): string {
  const abs = Math.abs(amount).toLocaleString('ko-KR');
  return amount < 0 ? `−${abs}` : abs;
}

/** UTC 저장값을 KST 날짜(YYYY-MM-DD)로. 서버·클라이언트 어디서 불려도 같은 결과를 낸다. */
export function formatDateKst(d: Date): string {
  return new Date(d.getTime() + KST_OFFSET_MS).toISOString().slice(0, 10);
}
```
`npm test` → PASS(총 5 tests: token 1 + format 4).
- [ ] **Step 8: 기존 화면 날짜 교체** — `app/groups/page.tsx`와 `app/groups/[groupId]/settings/page.tsx`의 `joinedAt.toISOString().slice(0, 10)`을 `formatDateKst(joinedAt)`으로 바꾼다.
- [ ] **Step 9: 검증·커밋** — lint/tsc/test(5)/build/`npm run e2e`(1) 통과 후 `git add -A; git commit -m "refactor: neon-serverless 드라이버·트랜잭션 도입 + Plan 01 부채 정리"`, push, CI 그린 확인.

---

### Task 2: 디자인 시스템 + Plan 01 화면 레트로핏 ⚠️사용자 게이트

**Files:** Create `components/ui/button.tsx`, `components/ui/field.tsx`, `components/ui/amount.tsx`, `components/ui/data-table.tsx`, `components/ui/page-header.tsx`. Modify `app/(auth)/login/page.tsx`, `app/groups/page.tsx`, `app/groups/new-group-form.tsx`, `app/groups/[groupId]/page.tsx`, `app/groups/[groupId]/settings/page.tsx`, `app/groups/[groupId]/settings/invite-link-panel.tsx`, `app/invite/[token]/page.tsx`, `app/invite/[token]/join-form.tsx`

**게이트:** 컨트롤러가 화면 시안(HTML 프로토타입 — 대시보드·회차 상세·지출 목록 3화면)을 만들어 사용자 승인을 받은 뒤 이 태스크를 시작한다. 승인된 시안이 아래 컴포넌트의 시각 기준이다.

- [ ] **Step 1: `components/ui/button.tsx`** — 스위스 그리드: 라운드 0, 블랙 채움/아웃라인 2단:
```tsx
import type { ComponentProps } from 'react';

type Props = ComponentProps<'button'> & { variant?: 'solid' | 'outline' };

export function Button({ variant = 'solid', className = '', ...rest }: Props) {
  const base = 'px-5 py-2.5 text-[14px] font-bold tracking-[0.04em] disabled:opacity-40';
  const skin =
    variant === 'solid'
      ? 'bg-ink text-paper'
      : 'border-2 border-ink text-ink';
  return <button className={`${base} ${skin} ${className}`} {...rest} />;
}
```
- [ ] **Step 2: `components/ui/field.tsx`** — 라벨+입력 한 쌍(괘선 하단만):
```tsx
import type { ComponentProps, ReactNode } from 'react';

type Props = ComponentProps<'input'> & { label: string; hint?: ReactNode };

export function Field({ label, hint, className = '', ...rest }: Props) {
  return (
    <label className="block">
      <span className="font-display text-[11px] font-bold tracking-[0.14em] text-muted uppercase">{label}</span>
      <input
        className={`mt-1.5 w-full border-b-2 border-ink bg-transparent py-2 text-[15px] outline-none focus:border-accent ${className}`}
        {...rest}
      />
      {hint ? <span className="mt-1 block text-[12px] text-muted">{hint}</span> : null}
    </label>
  );
}
```
- [ ] **Step 3: `components/ui/amount.tsx`** — 금액은 항상 등폭·부호 색:
```tsx
import { formatAmount } from '@/lib/format';

export function Amount({ value, size = 'md' }: { value: number; size?: 'md' | 'lg' | 'xl' }) {
  const sizes = { md: 'text-[15px]', lg: 'text-2xl', xl: 'text-4xl' } as const;
  return (
    <span className={`num ${sizes[size]} ${value < 0 ? 'text-ink' : 'text-accent'}`}>
      {formatAmount(value)}
    </span>
  );
}
```
(수입=오렌지, 지출=잉크 — 포인트 컬러는 화면당 한 의미만 갖는다는 방향 문서 규칙을 따른다.)
- [ ] **Step 4: `components/ui/data-table.tsx`** — 카드가 아니라 표. 2px 상단 괘선 + 1px 행 구분:
```tsx
import type { ReactNode } from 'react';

export function DataTable({ children }: { children: ReactNode }) {
  return <table className="w-full border-t-2 border-ink text-[14px]">{children}</table>;
}

export function Row({ children, testId }: { children: ReactNode; testId?: string }) {
  return <tr className="border-b border-hairline" data-testid={testId}>{children}</tr>;
}

export function Cell({ children, align = 'left' }: { children: ReactNode; align?: 'left' | 'right' }) {
  return <td className={`py-3 ${align === 'right' ? 'text-right' : ''}`}>{children}</td>;
}
```
- [ ] **Step 5: `components/ui/page-header.tsx`**:
```tsx
import Link from 'next/link';
import type { ReactNode } from 'react';

export function PageHeader({ back, title, right }: { back?: string; title: string; right?: ReactNode }) {
  return (
    <header className="flex items-center justify-between border-b-2 border-ink py-4">
      <div className="flex items-baseline gap-3">
        {back ? <Link href={back} className="font-display text-[12px] text-muted hover:text-accent">←</Link> : null}
        <h1 className="text-xl font-black tracking-tight" data-testid="page-title">{title}</h1>
      </div>
      {right}
    </header>
  );
}
```
- [ ] **Step 6: Plan 01 화면 레트로핏** — 위 컴포넌트로 8개 파일의 임시 Tailwind 마크업을 교체한다. **testid는 전부 그대로 유지**(E2E가 의존한다): `auth-*`, `group-name`, `group-display-name`, `group-create`, `group-list-item`, `group-title`, `group-balance`, `settings-link`, `member-row`, `invite-link`, `invite-copy`, `invite-regenerate`, `join-display-name`, `join-submit`, `join-login-link`. 각 화면은 `mx-auto max-w-3xl px-5` 컨테이너와 `PageHeader`를 쓴다.
- [ ] **Step 7: 검증·커밋** — lint/tsc/test/build + **`npm run e2e`(1 passing) 필수**(레트로핏이 셀렉터를 깨지 않았는지). 커밋 `feat: 디자인 시스템 컴포넌트 + 기존 화면 적용`, push, CI 그린.

---

### Task 3: 원장 도메인 (TDD)

**Files:** Create `lib/domain/ledger.ts`, `lib/domain/ledger.test.ts`

- [ ] **Step 1: 실패 테스트 먼저 — `lib/domain/ledger.test.ts`**:
```ts
import { describe, it, expect } from 'vitest';
import {
  balanceOf, signedAmount, reversalAmount, assertReversible, LedgerRuleError,
  type LedgerEntryLike,
} from '@/lib/domain/ledger';

const entry = (o: Partial<LedgerEntryLike>): LedgerEntryLike => ({
  id: 'e1', type: 'EXPENSE', amount: -1000, reversalOf: null, ...o,
});

describe('signedAmount', () => {
  it('회비 납부는 양수, 지출은 음수로 정규화한다', () => {
    expect(signedAmount('DUES_PAYMENT', 20000)).toBe(20000);
    expect(signedAmount('EXPENSE', 20000)).toBe(-20000);
  });
  it('0 이하 입력은 규칙 위반이다', () => {
    expect(() => signedAmount('EXPENSE', 0)).toThrow(LedgerRuleError);
    expect(() => signedAmount('EXPENSE', -1)).toThrow(LedgerRuleError);
  });
});

describe('balanceOf', () => {
  it('부호 있는 금액을 그대로 합산한다', () => {
    expect(balanceOf([entry({ amount: 360000 }), entry({ amount: -96000 }), entry({ amount: -52000 })])).toBe(212000);
  });
  it('빈 원장의 잔액은 0이다', () => {
    expect(balanceOf([])).toBe(0);
  });
  it('엔트리와 그 역분개의 합은 0이다', () => {
    const target = entry({ id: 'a', amount: -96000 });
    const rev = entry({ id: 'r', type: 'REVERSAL', amount: reversalAmount(target), reversalOf: 'a' });
    expect(balanceOf([target, rev])).toBe(0);
  });
});

describe('assertReversible', () => {
  it('평범한 엔트리는 역분개할 수 있다', () => {
    expect(() => assertReversible(entry({ id: 'a' }), [])).not.toThrow();
  });
  it('이미 역분개된 엔트리는 다시 역분개할 수 없다', () => {
    const target = entry({ id: 'a' });
    const existing = [entry({ id: 'r', type: 'REVERSAL', amount: 1000, reversalOf: 'a' })];
    expect(() => assertReversible(target, existing)).toThrow(LedgerRuleError);
  });
  it('역분개 엔트리 자체는 역분개 대상이 아니다', () => {
    const rev = entry({ id: 'r', type: 'REVERSAL', reversalOf: 'a' });
    expect(() => assertReversible(rev, [])).toThrow(LedgerRuleError);
  });
});
```
`npm test` → FAIL 확인.
- [ ] **Step 2: `lib/domain/ledger.ts`**:
```ts
export type LedgerType = 'DUES_PAYMENT' | 'EXPENSE' | 'REVERSAL';

export type LedgerEntryLike = {
  id: string;
  type: LedgerType;
  /** 부호 있는 정수 원. 수입 +, 지출 −. */
  amount: number;
  reversalOf: string | null;
};

export class LedgerRuleError extends Error {
  constructor(public code: 'INVALID_AMOUNT' | 'ALREADY_REVERSED' | 'NOT_REVERSIBLE') {
    super(code);
  }
}

/** 사용자가 입력한 양수 금액을 종류에 맞는 부호로 정규화한다. */
export function signedAmount(type: 'DUES_PAYMENT' | 'EXPENSE', input: number): number {
  if (!Number.isInteger(input) || input <= 0) throw new LedgerRuleError('INVALID_AMOUNT');
  return type === 'EXPENSE' ? -input : input;
}

/** 잔액은 저장하지 않는다 — 언제나 원장 합산으로만 구한다 (ADR-001). */
export function balanceOf(entries: readonly LedgerEntryLike[]): number {
  return entries.reduce((sum, e) => sum + e.amount, 0);
}

/** 역분개 엔트리의 금액 = 대상의 정확한 반대. */
export function reversalAmount(target: LedgerEntryLike): number {
  return -target.amount;
}

/** 역분개 가능 여부. 같은 대상에 두 번, 역분개의 역분개는 금지한다. */
export function assertReversible(
  target: LedgerEntryLike,
  groupEntries: readonly LedgerEntryLike[],
): void {
  if (target.type === 'REVERSAL') throw new LedgerRuleError('NOT_REVERSIBLE');
  if (groupEntries.some((e) => e.reversalOf === target.id)) throw new LedgerRuleError('ALREADY_REVERSED');
}
```
`npm test` → PASS(총 13 tests).
- [ ] **Step 3: 커밋** — `feat: 원장 도메인 — 부호 규칙·잔액 합산·역분개 검증(ADR-001)`, push, CI 그린.

---

### Task 4: 원장 스키마 + 대시보드 잔액 연결

**Files:** Modify `lib/db/schema.ts`, `lib/db/queries.ts`, `app/groups/[groupId]/page.tsx`

- [ ] **Step 1: 스키마 추가** — `lib/db/schema.ts`에 append:
```ts
export const ledgerEntries = pgTable('ledger_entries', {
  id: text('id').primaryKey(),
  groupId: text('group_id').notNull().references(() => groups.id),
  type: text('type', { enum: ['DUES_PAYMENT', 'EXPENSE', 'REVERSAL'] }).notNull(),
  // 부호 있는 정수 원 — 수입 +, 지출 −. 잔액은 이 컬럼의 합이다(저장 금지).
  amount: integer('amount').notNull(),
  occurredAt: timestamp('occurred_at', { withTimezone: true }).notNull(),
  category: text('category'),
  memo: text('memo'),
  createdBy: text('created_by').notNull().references(() => user.id),
  // 역분개 대상. 같은 대상에 둘 이상 달릴 수 없다.
  reversalOf: text('reversal_of').unique(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});
```
`integer`를 `drizzle-orm/pg-core` import에 추가한다. (Plan 01 Task 2에서 미사용이라 뺐던 그 import다.)
- [ ] **Step 2: 적용** — `npm run db:push`(dev), `npm run db:push:test`. 두 브랜치 모두 `Changes applied` 확인. **test 브랜치는 push 후 센티널이 사라지므로** `create table if not exists e2e_sentinel();`를 다시 실행한다(플랜 01 Task 8 구현 노트 참조).
- [ ] **Step 3: `lib/db/queries.ts`에 추가**:
```ts
import { desc, eq, sum } from 'drizzle-orm';
import { ledgerEntries } from './schema';

/** 잔액 = 원장 합산(파생값). 엔트리가 없으면 0. */
export async function getBalance(groupId: string): Promise<number> {
  const [row] = await db
    .select({ total: sum(ledgerEntries.amount) })
    .from(ledgerEntries)
    .where(eq(ledgerEntries.groupId, groupId));
  return Number(row?.total ?? 0);
}

export async function getRecentEntries(groupId: string, limit = 10) {
  return db
    .select()
    .from(ledgerEntries)
    .where(eq(ledgerEntries.groupId, groupId))
    .orderBy(desc(ledgerEntries.occurredAt), desc(ledgerEntries.createdAt))
    .limit(limit);
}
```
- [ ] **Step 4: 대시보드 연결** — `app/groups/[groupId]/page.tsx`의 하드코딩된 `0원`을 `getBalance` 결과로 바꾸고 `<Amount value={balance} size="xl" />`로 렌더한다. `group-balance` testid는 유지한다(E2E는 "0원"이 아니라 숫자 존재를 본다 — 필요하면 E2E 문구를 `0`으로 완화하고 보고한다). 최근 내역 섹션은 `getRecentEntries`로 `DataTable` 렌더(비어 있으면 "아직 기록이 없습니다").
- [ ] **Step 5: 검증·커밋** — lint/tsc/test/build/e2e. 커밋 `feat: 원장 스키마 + 대시보드 잔액(원장 합산)`, push, CI 그린.

---

### Task 5: 지출 기록

**Files:** Create `actions/ledger.ts`, `app/groups/[groupId]/expenses/page.tsx`, `app/groups/[groupId]/expenses/expense-form.tsx`

- [ ] **Step 1: `actions/ledger.ts`**:
```ts
'use server';
import { z } from 'zod';
import { revalidatePath } from 'next/cache';
import { groupActionClient, assertOwner, ActionError } from './clients';
import { db } from '@/lib/db';
import { ledgerEntries } from '@/lib/db/schema';
import { signedAmount, LedgerRuleError } from '@/lib/domain/ledger';

export const createExpense = groupActionClient
  .inputSchema(
    z.object({
      groupId: z.string(),
      amount: z.number().int().positive().max(100_000_000),
      occurredOn: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
      category: z.string().max(20).optional(),
      memo: z.string().max(100).optional(),
    }),
  )
  .action(async ({ parsedInput, ctx }) => {
    assertOwner(ctx.role);
    let amount: number;
    try {
      amount = signedAmount('EXPENSE', parsedInput.amount);
    } catch (e) {
      throw e instanceof LedgerRuleError ? new ActionError(e.code) : e;
    }
    // 입력은 KST 날짜(YYYY-MM-DD) — 그날 정오(UTC 03:00)로 저장해 시간대 경계에서 날짜가 밀리지 않게 한다.
    const occurredAt = new Date(`${parsedInput.occurredOn}T03:00:00.000Z`);
    await db.insert(ledgerEntries).values({
      id: crypto.randomUUID(),
      groupId: ctx.groupId,
      type: 'EXPENSE',
      amount,
      occurredAt,
      category: parsedInput.category || null,
      memo: parsedInput.memo || null,
      createdBy: ctx.userId,
      reversalOf: null,
    });
    revalidatePath(`/groups/${ctx.groupId}`);
    revalidatePath(`/groups/${ctx.groupId}/expenses`);
    return { ok: true };
  });
```
- [ ] **Step 2: 지출 화면** — `app/groups/[groupId]/expenses/page.tsx`(RSC): 세션·멤버십 게이트는 대시보드와 동일(`getGroupForMember` → `notFound()`), 지출 엔트리 목록(`type`이 EXPENSE 또는 그 REVERSAL인 것)을 `DataTable`로, 총무면 `ExpenseForm`을 상단에 렌더. testids: `expense-row`, `expense-amount`, `expense-form`.
- [ ] **Step 3: `expense-form.tsx`**(client): `Field` 3개(금액·날짜·메모) + 카테고리 텍스트, `useAction(createExpense)`, `isPending` 가드, 한국어 에러(`FORBIDDEN`은 "총무만 기록할 수 있습니다", `INVALID_AMOUNT`는 "금액은 1원 이상이어야 합니다"). 성공 시 폼 초기화(`reset()`), 목록은 `revalidatePath`로 갱신. testids: `expense-amount-input`, `expense-date`, `expense-memo`, `expense-submit`.
- [ ] **Step 4: 수동 확인** — dev에서 총무 계정으로 지출 3건 기록 → 대시보드 잔액이 음수로 내려가는지, 목록 정렬(최신순)이 맞는지 확인. 멤버 계정으로 폼이 안 보이고 액션 직접 호출 시 FORBIDDEN인지 확인.
- [ ] **Step 5: 검증·커밋** — 전 게이트 + e2e. 커밋 `feat: 지출 기록(F3)`, push, CI 그린.

---

### Task 6: 역분개 (정정)

**Files:** Modify `actions/ledger.ts`, `app/groups/[groupId]/expenses/page.tsx`. Create `app/groups/[groupId]/expenses/reverse-button.tsx`

- [ ] **Step 1: `reverseEntry` 액션** — `actions/ledger.ts`에 추가:
```ts
export const reverseEntry = groupActionClient
  .inputSchema(z.object({ groupId: z.string(), entryId: z.string() }))
  .action(async ({ parsedInput, ctx }) => {
    assertOwner(ctx.role);
    const entries = await db
      .select({ id: ledgerEntries.id, type: ledgerEntries.type, amount: ledgerEntries.amount, reversalOf: ledgerEntries.reversalOf })
      .from(ledgerEntries)
      .where(eq(ledgerEntries.groupId, ctx.groupId));
    const target = entries.find((e) => e.id === parsedInput.entryId);
    if (!target) throw new ActionError('ENTRY_NOT_FOUND'); // 다른 모임의 엔트리도 여기로 떨어진다
    try {
      assertReversible(target, entries);
    } catch (e) {
      throw e instanceof LedgerRuleError ? new ActionError(e.code) : e;
    }
    await db.insert(ledgerEntries).values({
      id: crypto.randomUUID(),
      groupId: ctx.groupId,
      type: 'REVERSAL',
      amount: reversalAmount(target),
      occurredAt: new Date(),
      category: null,
      memo: `정정: ${parsedInput.entryId.slice(0, 8)}`,
      createdBy: ctx.userId,
      reversalOf: target.id,
    });
    revalidatePath(`/groups/${ctx.groupId}`);
    revalidatePath(`/groups/${ctx.groupId}/expenses`);
    return { ok: true };
  });
```
import에 `eq`, `assertReversible`, `reversalAmount`를 추가한다. **그룹 스코프 조회로 타 모임 엔트리 접근을 원천 차단**하는 것이 이 액션의 보안 핵심이다.
- [ ] **Step 2: `reverse-button.tsx`**(client): `window.confirm('이 기록을 정정할까요? 원장에는 취소 기록이 남습니다.')` 후 `useAction(reverseEntry)`, `isPending` 가드, testid `reverse-button`. 이미 역분개된 행에는 버튼 대신 "정정됨" 뱃지를 표시한다(목록 렌더에서 `reversalOf` 집합으로 판정).
- [ ] **Step 3: 수동 확인** — 지출 1건 역분개 → 잔액이 원복되는지, 같은 건 재역분개 시도 시 ALREADY_REVERSED로 막히는지, 역분개 행 자체에는 버튼이 없는지.
- [ ] **Step 4: 검증·커밋** — 전 게이트 + e2e. 커밋 `feat: 역분개로 정정(ADR-001)`, push, CI 그린.

---

### Task 7: 회비 도메인 (TDD)

**Files:** Create `lib/domain/dues.ts`, `lib/domain/dues.test.ts`

- [ ] **Step 1: 실패 테스트 — `lib/domain/dues.test.ts`**:
```ts
import { describe, it, expect } from 'vitest';
import { unpaidMembers, roundTotals, parsePeriod, DuesRuleError } from '@/lib/domain/dues';

const members = [
  { membershipId: 'm1', displayName: '민지' },
  { membershipId: 'm2', displayName: '철수' },
  { membershipId: 'm3', displayName: '영희' },
];

describe('unpaidMembers', () => {
  it('납부 기록이 없는 멤버만 골라낸다', () => {
    expect(unpaidMembers(members, ['m2'])).toEqual([
      { membershipId: 'm1', displayName: '민지' },
      { membershipId: 'm3', displayName: '영희' },
    ]);
  });
  it('전원 납부면 빈 배열이다', () => {
    expect(unpaidMembers(members, ['m1', 'm2', 'm3'])).toEqual([]);
  });
});

describe('roundTotals', () => {
  it('예상 총액·수납액·미납액을 계산한다', () => {
    expect(roundTotals(20000, 3, ['m1', 'm2'])).toEqual({ expected: 60000, collected: 40000, outstanding: 20000 });
  });
});

describe('parsePeriod', () => {
  it('YYYY-MM을 받아 그대로 돌려준다', () => {
    expect(parsePeriod('2026-01')).toBe('2026-01');
  });
  it('형식이 어긋나면 규칙 위반이다', () => {
    expect(() => parsePeriod('2026-1')).toThrow(DuesRuleError);
    expect(() => parsePeriod('2026-13')).toThrow(DuesRuleError);
  });
});
```
`npm test` → FAIL 확인.
- [ ] **Step 2: `lib/domain/dues.ts`**:
```ts
export class DuesRuleError extends Error {
  constructor(public code: 'INVALID_PERIOD') {
    super(code);
  }
}

export type MemberLike = { membershipId: string; displayName: string };

/** 회차 기간은 'YYYY-MM' 문자열로만 다룬다 — 월 단위 비교·정렬이 문자열로 끝난다. */
export function parsePeriod(input: string): string {
  if (!/^\d{4}-(0[1-9]|1[0-2])$/.test(input)) throw new DuesRuleError('INVALID_PERIOD');
  return input;
}

export function unpaidMembers(members: readonly MemberLike[], paidIds: readonly string[]): MemberLike[] {
  const paid = new Set(paidIds);
  return members.filter((m) => !paid.has(m.membershipId));
}

export function roundTotals(amountPerPerson: number, memberCount: number, paidIds: readonly string[]) {
  const expected = amountPerPerson * memberCount;
  const collected = amountPerPerson * paidIds.length;
  return { expected, collected, outstanding: expected - collected };
}
```
`npm test` → PASS(총 19 tests).
- [ ] **Step 3: 커밋** — `feat: 회비 도메인 — 회차 기간·미납자·집계`, push, CI 그린.

---

### Task 8: 회차 생성·목록

**Files:** Modify `lib/db/schema.ts`, `lib/db/queries.ts`. Create `actions/dues.ts`, `app/groups/[groupId]/dues/page.tsx`, `app/groups/[groupId]/dues/round-form.tsx`

- [ ] **Step 1: 스키마 추가**:
```ts
export const duesRounds = pgTable('dues_rounds', {
  id: text('id').primaryKey(),
  groupId: text('group_id').notNull().references(() => groups.id),
  period: text('period').notNull(), // 'YYYY-MM'
  amountPerPerson: integer('amount_per_person').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
}, (t) => [uniqueIndex('dues_rounds_group_period').on(t.groupId, t.period)]);

export const duesPayments = pgTable('dues_payments', {
  id: text('id').primaryKey(),
  roundId: text('round_id').notNull().references(() => duesRounds.id),
  membershipId: text('membership_id').notNull().references(() => memberships.id),
  // 이 납부가 만든 원장 엔트리. 납부 취소 시 이 행은 지우고 원장에는 역분개를 남긴다.
  ledgerEntryId: text('ledger_entry_id').notNull().references(() => ledgerEntries.id),
  paidAt: timestamp('paid_at', { withTimezone: true }).notNull().defaultNow(),
}, (t) => [uniqueIndex('dues_payments_round_membership').on(t.roundId, t.membershipId)]);
```
`npm run db:push` + `npm run db:push:test` + **센티널 재생성**.
- [ ] **Step 2: `createRound` 액션 — `actions/dues.ts`**:
```ts
'use server';
import { z } from 'zod';
import { and, eq } from 'drizzle-orm';
import { revalidatePath } from 'next/cache';
import { groupActionClient, assertOwner, ActionError } from './clients';
import { db } from '@/lib/db';
import { duesRounds } from '@/lib/db/schema';
import { parsePeriod, DuesRuleError } from '@/lib/domain/dues';

export const createRound = groupActionClient
  .inputSchema(
    z.object({
      groupId: z.string(),
      period: z.string(),
      amountPerPerson: z.number().int().positive().max(10_000_000),
    }),
  )
  .action(async ({ parsedInput, ctx }) => {
    assertOwner(ctx.role);
    let period: string;
    try {
      period = parsePeriod(parsedInput.period);
    } catch (e) {
      throw e instanceof DuesRuleError ? new ActionError(e.code) : e;
    }
    const dup = await db.query.duesRounds.findFirst({
      where: and(eq(duesRounds.groupId, ctx.groupId), eq(duesRounds.period, period)),
    });
    if (dup) throw new ActionError('ROUND_EXISTS');
    const id = crypto.randomUUID();
    await db.insert(duesRounds).values({ id, groupId: ctx.groupId, period, amountPerPerson: parsedInput.amountPerPerson });
    revalidatePath(`/groups/${ctx.groupId}/dues`);
    return { roundId: id };
  });
```
- [ ] **Step 3: 회차 목록 화면** — `app/groups/[groupId]/dues/page.tsx`: 회차 목록(기간 내림차순, 1인 금액, 납부 n/N, 수납액)을 `DataTable`로. 각 행은 `/groups/[groupId]/dues/[roundId]`로 링크. 총무면 `RoundForm`(기간 `month` 입력 + 금액) 노출. `lib/db/queries.ts`에 `getRoundsWithCounts(groupId)` 추가(회차별 납부 수를 서브쿼리/조인으로 한 번에). testids: `round-row`, `round-period`, `round-create`.
- [ ] **Step 4: 수동 확인** — 회차 2개 생성, 같은 기간 재생성 시 ROUND_EXISTS 에러 문구 확인.
- [ ] **Step 5: 검증·커밋** — 전 게이트 + e2e. 커밋 `feat: 회비 회차 생성·목록(F2 전반)`, push, CI 그린.

---

### Task 9: 납부 체크 (트랜잭션 2-insert)

**Files:** Modify `actions/dues.ts`, `lib/db/queries.ts`. Create `app/groups/[groupId]/dues/[roundId]/page.tsx`, `app/groups/[groupId]/dues/[roundId]/payment-toggle.tsx`

- [ ] **Step 1: `markPaid` — 원장 엔트리와 납부 기록을 한 트랜잭션으로**:
```ts
export const markPaid = groupActionClient
  .inputSchema(z.object({ groupId: z.string(), roundId: z.string(), membershipId: z.string() }))
  .action(async ({ parsedInput, ctx }) => {
    assertOwner(ctx.role);
    const round = await db.query.duesRounds.findFirst({
      where: and(eq(duesRounds.id, parsedInput.roundId), eq(duesRounds.groupId, ctx.groupId)),
    });
    if (!round) throw new ActionError('ROUND_NOT_FOUND');
    const member = await db.query.memberships.findFirst({
      where: and(eq(memberships.id, parsedInput.membershipId), eq(memberships.groupId, ctx.groupId)),
    });
    if (!member) throw new ActionError('NOT_MEMBER');

    const entryId = crypto.randomUUID();
    try {
      await db.transaction(async (tx) => {
        await tx.insert(ledgerEntries).values({
          id: entryId,
          groupId: ctx.groupId,
          type: 'DUES_PAYMENT',
          amount: signedAmount('DUES_PAYMENT', round.amountPerPerson),
          occurredAt: new Date(),
          category: '회비',
          memo: `${round.period} ${member.displayName}`,
          createdBy: ctx.userId,
          reversalOf: null,
        });
        await tx.insert(duesPayments).values({
          id: crypto.randomUUID(),
          roundId: round.id,
          membershipId: member.id,
          ledgerEntryId: entryId,
        });
      });
    } catch (e) {
      // 중복 체크(같은 회차·멤버)는 유니크 인덱스가 막는다 — 멱등 성공으로 처리한다.
      if ((e as { code?: string }).code === '23505') return { already: true };
      throw e;
    }
    revalidatePath(`/groups/${ctx.groupId}`);
    revalidatePath(`/groups/${ctx.groupId}/dues/${round.id}`);
    return { already: false };
  });
```
- [ ] **Step 2: `unmarkPaid` — 납부 취소는 행 삭제 + 역분개**:
```ts
export const unmarkPaid = groupActionClient
  .inputSchema(z.object({ groupId: z.string(), roundId: z.string(), membershipId: z.string() }))
  .action(async ({ parsedInput, ctx }) => {
    assertOwner(ctx.role);
    const [payment] = await db
      .select({ id: duesPayments.id, ledgerEntryId: duesPayments.ledgerEntryId })
      .from(duesPayments)
      .innerJoin(duesRounds, eq(duesPayments.roundId, duesRounds.id))
      .where(and(
        eq(duesPayments.roundId, parsedInput.roundId),
        eq(duesPayments.membershipId, parsedInput.membershipId),
        eq(duesRounds.groupId, ctx.groupId), // 타 모임 침범 차단
      ));
    if (!payment) throw new ActionError('PAYMENT_NOT_FOUND');
    const [entry] = await db
      .select({ id: ledgerEntries.id, type: ledgerEntries.type, amount: ledgerEntries.amount, reversalOf: ledgerEntries.reversalOf })
      .from(ledgerEntries)
      .where(eq(ledgerEntries.id, payment.ledgerEntryId));

    await db.transaction(async (tx) => {
      await tx.insert(ledgerEntries).values({
        id: crypto.randomUUID(),
        groupId: ctx.groupId,
        type: 'REVERSAL',
        amount: reversalAmount(entry),
        occurredAt: new Date(),
        category: '회비',
        memo: '납부 취소',
        createdBy: ctx.userId,
        reversalOf: entry.id,
      });
      await tx.delete(duesPayments).where(eq(duesPayments.id, payment.id));
    });
    revalidatePath(`/groups/${ctx.groupId}`);
    revalidatePath(`/groups/${ctx.groupId}/dues/${parsedInput.roundId}`);
    return { ok: true };
  });
```
(원장은 +금액·−금액 두 줄로 이력이 남고, `dues_payments`는 "지금 납부 상태"만 들고 있다 — 재납부가 가능해진다.)
- [ ] **Step 3: 회차 상세 화면** — `app/groups/[groupId]/dues/[roundId]/page.tsx`: 회차 헤더(기간·1인 금액·`roundTotals` 결과 3수치), 멤버×납부 체크 목록(`DataTable` + 행마다 `PaymentToggle`), 하단에 `unpaidMembers` 기반 미납자 이름 나열. 멤버(비총무)에게는 토글 없이 상태만 보인다. testids: `round-total-expected`, `round-total-collected`, `round-total-outstanding`, `payment-row`, `payment-toggle`.
- [ ] **Step 4: `payment-toggle.tsx`**(client): 현재 납부 여부에 따라 `markPaid`/`unmarkPaid` 호출, `isPending` 가드, 취소는 `confirm('납부를 취소할까요? 원장에는 취소 기록이 남습니다.')`.
- [ ] **Step 5: 수동 확인(정합성)** — 회차 생성(1인 20,000) → 멤버 2명 중 1명 체크 → 대시보드 잔액 +20,000 → 취소 → 잔액 0 → 원장에 +20,000/−20,000 두 줄 확인 → 재체크 성공(유니크 위반 없음).
- [ ] **Step 6: 검증·커밋** — 전 게이트 + e2e. 커밋 `feat: 회비 납부 체크·취소(F2 완결)`, push, CI 그린.

---

### Task 10: E2E 확장 + 배포 검증

**Files:** Create `e2e/ledger.spec.ts`. Modify `README.md`

- [ ] **Step 1: `e2e/ledger.spec.ts`** — 돈 정합성을 눈이 아니라 테스트로 고정한다:
```ts
import { test, expect } from '@playwright/test';

test('지출·회비가 잔액에 정확히 반영되고 정정으로 되돌아온다', async ({ page }) => {
  const ts = Date.now();
  // 1) 총무 가입 + 모임 생성 (foundation.spec.ts와 같은 절차)
  await page.goto('/login');
  // ... auth-toggle → auth-name/email/password → auth-submit
  // ... group-name '정산모임' / group-display-name '민지' → group-create
  await expect(page.getByTestId('group-balance')).toContainText('0');

  // 2) 지출 96,000 → 잔액 −96,000
  await page.getByRole('link', { name: '지출' }).click();
  await page.getByTestId('expense-amount-input').fill('96000');
  await page.getByTestId('expense-date').fill('2026-01-15');
  await page.getByTestId('expense-memo').fill('코트 대관');
  await page.getByTestId('expense-submit').click();
  await expect(page.getByTestId('expense-row')).toHaveCount(1);

  // 3) 회비 회차 20,000 + 본인 납부 → 잔액 −76,000
  await page.goto(`/groups/${'...'}/dues`); // 대시보드 링크로 이동해 URL을 얻는다
  await page.getByTestId('round-create').click();
  await expect(page.getByTestId('round-row')).toHaveCount(1);
  await page.getByTestId('round-row').click();
  await page.getByTestId('payment-toggle').first().click();
  await expect(page.getByTestId('round-total-collected')).toContainText('20,000');

  // 4) 지출 정정 → 잔액 +20,000 (회비만 남는다)
  await page.goto(`/groups/${'...'}/expenses`);
  await page.getByTestId('reverse-button').first().click();
  // confirm 다이얼로그 자동 수락
  await expect(page.getByTestId('expense-row')).toHaveCount(2); // 원본 + 정정
});
```
**작성 규칙:** 위 골격의 *검증 포인트*(각 단계의 잔액·행 수·수납액)는 유지하되, 셀렉터·URL 획득 방식은 실제 DOM에 맞춰 채운다. `page.on('dialog', d => d.accept())`로 confirm을 처리한다. 최종 잔액 단언(`group-balance`)을 각 단계마다 넣어 **원장 합산이 화면과 어긋나지 않음**을 증명하는 것이 이 스펙의 존재 이유다.
- [ ] **Step 2: 로컬 e2e 전체** — `npm run e2e` → 2 passing(foundation + ledger).
- [ ] **Step 3: 배포 검증** — push 후 Vercel 자동배포 완료를 기다렸다가 프로덕션에서 스모크: 새 계정 가입 → 모임 생성 → 지출 1건 → 대시보드 잔액이 음수로 표시되는지. (프로덕션 DB는 Neon `production` 브랜치이므로 테스트 데이터가 남는다 — 스모크 후 해당 모임을 지우거나, 그대로 두고 README에 기록한다.)
- [ ] **Step 4: README 갱신** — v1 범위 체크리스트에서 F2·F3을 완료로 표시하고, 남은 F4~F7이 Plan 03임을 명시. 커밋 `test: 원장 정합성 E2E + 문서 갱신`, push, CI 그린.

---

## Self-Review

**1. 스펙 커버리지** — requirements.md의 F2(회비 회차·납부 체크: Task 8·9), F3(지출 기록·역분개 정정: Task 5·6)을 모두 덮는다. F1은 Plan 01에서 완료. F4(정산)·F5(공개 장부)·F6(미납 복붙)·F7(CSV)은 **의도적으로 Plan 03**으로 남긴다 — 이 플랜만으로도 "걷고 쓰는" 서비스가 배포 가능한 상태가 된다. ADR-001(불변 원장·역분개·파생 잔액)은 Task 3·4·6·9가, ADR-002(전 쓰기 인가)는 모든 액션의 `groupActionClient`+`assertOwner`가 구현한다.

**2. 플레이스홀더 스캔** — Task 10의 E2E는 셀렉터·URL 획득을 실제 DOM에 위임했으나 검증 포인트를 수치까지 명시했고, Task 2·5·8의 화면 마크업은 컴포넌트·testid·게이트 조건을 확정해 위임 범위를 좁혔다. 그 외 모든 도메인·액션·스키마 코드는 완전한 형태로 수록했다. Task 2는 사용자 승인 게이트임을 문서 상단과 태스크 제목에 모두 표시했다.

**3. 타입 일관성** — `LedgerEntryLike`(T3 정의 → T6·T9 사용), `signedAmount`(T3 → T5·T9), `reversalAmount`/`assertReversible`(T3 → T6·T9), `parsePeriod`/`unpaidMembers`/`roundTotals`(T7 → T8·T9), `formatAmount`/`formatDateKst`(T1 → T2 `Amount` 컴포넌트·전 화면), `Amount`/`DataTable`/`Field`/`Button`/`PageHeader`(T2 → T4·T5·T8·T9). 스키마 컬럼명은 glossary의 snake_case 규칙을 따르고 TS는 camelCase로 노출한다.

**4. 알려진 리스크** — ① `db:push`가 test 브랜치의 `e2e_sentinel`을 지우므로 스키마를 바꾸는 Task 4·8에 재생성 단계를 명시했다. ② neon-serverless 전환은 F1 전체에 영향을 주므로 Task 1 Step 3에서 회귀를 먼저 확인하고 실패 시 멈춘다. ③ 프로덕션 스모크가 실데이터에 남는 문제를 Task 10에서 명시적으로 다룬다.
